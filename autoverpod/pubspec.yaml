name: autoverpod
description: Annotations for Riverpod widgets to simplify code generation and automate widget creation.
version: 0.0.6
repository: https://github.com/pckimlong/autoverpod_library
homepage: https://github.com/pckimlong/autoverpod_library
issue_tracker: https://github.com/pckimlong/autoverpod_library/issues

environment:
  sdk: ">=3.6.0 <4.0.0"
resolution: workspace

dependencies:
  meta: ^1.16.0
  riverpod: ^3.0.0

dev_dependencies:
  lints: ^6.0.0
  # test removed to avoid analyzer conflict with source_gen/analyzer upgrades
